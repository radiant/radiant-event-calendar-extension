- if event
  %h3.event
    = event.title
  
  %p.date
    = event.starts
    on 
    = event.date
    - if event.event_venue
      at 
      = link_to event.event_venue.title, event.event_venue.url
    - else
      at
      = event.location

  %p.description
    = event.short_description
